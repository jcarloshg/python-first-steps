### create enviroments with python

```zsh
python3 -m venv ~/venv
source ~/venv/bin/activate
pip install requests
```

### pipx

```zsh
# install pipx
sudo apt install pipx
pipx install requests

pipx install pipenv
pipx ensurepath
```
